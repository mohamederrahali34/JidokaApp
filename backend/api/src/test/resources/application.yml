spring:
  datasource:
    url: jdbc:h2:mem:retreeb-consumer-wallet
    driver-class-name: org.h2.Driver
    username: user
    password: password

  liquibase:
    enabled: false

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate.format_sql: true
  sql:
    init:
      platform: org.hibernate.dialect.H2Dialect
  main:
    banner-mode: off

security:
  jwt:
    secret: ${JWT_SECRET:zdtlD3JK56m6wTTgsNFhqzjqP}
    issuer: ${JWT_ISSUER:nimbleways}

server:
  servlet:
    context-path: /api

logging:
  level:
    org.springframework: ERROR
    root: ERROR

retrofit:
  posts:
    base-url: https://jsonplaceholder.typicode.com
